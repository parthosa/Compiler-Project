<mainFunction> --> 
<stmtsAndFunctionDefs> --> END, 
<stmtOrFunctionDef> --> END, FUNCTION, ID, SQO, IF, READ, PRINT, INT, REAL, STRING, MATRIX, FUNID, 
<stmtsAndFunctionDefsOrEmpty> --> END, 
<stmt> --> END, ENDIF, ELSE, ID, SQO, IF, READ, PRINT, INT, REAL, STRING, MATRIX, FUNID, FUNCTION, 
<functionDef> --> END, FUNCTION, ID, SQO, IF, READ, PRINT, INT, REAL, STRING, MATRIX, FUNID, 
<declarationStmt> --> END, ENDIF, ELSE, ID, SQO, IF, READ, PRINT, INT, REAL, STRING, MATRIX, FUNID, FUNCTION, 
<assignmentStmt> --> END, ENDIF, ELSE, ID, SQO, IF, READ, PRINT, INT, REAL, STRING, MATRIX, FUNID, FUNCTION, 
<conditionalStmt> --> END, ENDIF, ELSE, ID, SQO, IF, READ, PRINT, INT, REAL, STRING, MATRIX, FUNID, FUNCTION, 
<ioStmt> --> END, ENDIF, ELSE, ID, SQO, IF, READ, PRINT, INT, REAL, STRING, MATRIX, FUNID, FUNCTION, 
<funcStmt> --> END, ENDIF, ELSE, ID, SQO, IF, READ, PRINT, INT, REAL, STRING, MATRIX, FUNID, FUNCTION, 
<otherStmts> --> ENDIF, ELSE, 
<funCall> --> SEMICOLON, 
<type> --> ID, 
<idList> --> SEMICOLON, SQC, CL, 
<rightHandSideOfId> --> SEMICOLON, 
<rightHandSideOfTuple> --> SEMICOLON, 
<arithmeticExpression> --> SEMICOLON, CL, 
<booleanExpression> --> CL, ID, SQO, IF, READ, PRINT, INT, REAL, STRING, MATRIX, FUNID, 
<elseStmt> --> ENDIF, 
<matrix> --> SEMICOLON, CL, PLUS, MINUS, MUL, DIV, 
<numberList> --> SQC, SEMICOLON, 
<moreMatrixNums> --> SQC, 
<moreNums> --> SQC, SEMICOLON, 
<term> --> SEMICOLON, CL, PLUS, MINUS, 
<arithmeticExpressionOps> --> SEMICOLON, CL, 
<plusOrMinus> --> OP, ID, NUM, RNUM, SIZE, STR, SQO, 
<factor> --> SEMICOLON, CL, PLUS, MINUS, MUL, DIV, 
<termOps> --> SEMICOLON, CL, PLUS, MINUS, 
<mulOrDiv> --> OP, ID, NUM, RNUM, SIZE, STR, SQO, 
<var> --> SEMICOLON, CL, PLUS, MINUS, MUL, DIV, 
<binaryLogicalOp> --> OP, 
<constrainedVars> --> CL, LT, LE, EQ, GT, GE, NE, ID, SQO, IF, READ, PRINT, INT, REAL, STRING, MATRIX, FUNID, 
<relationalOp> --> ID, NUM, RNUM, 
<parameterList> --> SQC, 
<remainingList> --> SQC, 
<funIdList> --> CL, 
<matrixRest> --> SEMICOLON, CL, PLUS, MINUS, MUL, DIV, 
<moreIds> --> SEMICOLON, SQC, CL, 
