<mainFunction> --> MAIN, 
<stmtsAndFunctionDefs> --> FUNCTION, ID, SQO, IF, READ, PRINT, INT, REAL, STRING, MATRIX, FUNID, 
<stmtOrFunctionDef> --> FUNCTION, ID, SQO, IF, READ, PRINT, INT, REAL, STRING, MATRIX, FUNID, 
<stmtsAndFunctionDefsOrEmpty> --> _eps_, FUNCTION, ID, SQO, IF, READ, PRINT, INT, REAL, STRING, MATRIX, FUNID, 
<stmt> --> ID, SQO, IF, READ, PRINT, INT, REAL, STRING, MATRIX, FUNID, 
<functionDef> --> FUNCTION, 
<declarationStmt> --> INT, REAL, STRING, MATRIX, 
<assignmentStmt> --> ID, SQO, 
<conditionalStmt> --> IF, 
<ioStmt> --> READ, PRINT, 
<funcStmt> --> FUNID, 
<otherStmts> --> _eps_, ID, SQO, IF, READ, PRINT, INT, REAL, STRING, MATRIX, FUNID, 
<funCall> --> FUNID, 
<type> --> INT, REAL, STRING, MATRIX, 
<idList> --> ID, 
<rightHandSideOfId> --> FUNID, OP, ID, NUM, RNUM, SIZE, STR, SQO, 
<rightHandSideOfTuple> --> FUNID, SIZE, 
<arithmeticExpression> --> OP, ID, NUM, RNUM, SIZE, STR, SQO, 
<booleanExpression> --> OP, NOT, ID, NUM, RNUM, 
<elseStmt> --> ELSE, _eps_, 
<matrix> --> SQO, 
<numberList> --> NUM, 
<moreMatrixNums> --> SEMICOLON, _eps_, 
<moreNums> --> COMMA, _eps_, 
<term> --> OP, ID, NUM, RNUM, SIZE, STR, SQO, 
<arithmeticExpressionOps> --> _eps_, PLUS, MINUS, 
<plusOrMinus> --> PLUS, MINUS, 
<factor> --> OP, ID, NUM, RNUM, SIZE, STR, SQO, 
<termOps> --> _eps_, MUL, DIV, 
<mulOrDiv> --> MUL, DIV, 
<var> --> ID, NUM, RNUM, SIZE, STR, SQO, 
<binaryLogicalOp> --> AND, OR, 
<constrainedVars> --> ID, NUM, RNUM, 
<relationalOp> --> LT, LE, EQ, GT, GE, NE, 
<parameterList> --> INT, REAL, STRING, MATRIX, _eps_, 
<remainingList> --> COMMA, _eps_, 
<funIdList> --> ID, _eps_, 
<matrixRest> --> SQO, _eps_, 
<moreIds> --> COMMA, _eps_, 
